import src.neural_network as nn
from src.network_save import test_save

if __name__ == "__main__":
    test_save()
    # neural_net = nn.NeuralNetwork(2, 3, 2)
    # c = neural_net.train(1, [[1, 2]])
